---
paths:
  - "**/*.ts"
  - "**/*.tsx"
  - "**/*.js"
  - "**/*.jsx"
  - "**/*.css"
---

# Project Standard

项目通用要求如下：

## 通用约束

1. 必要的注释需要加上
2. 能使用unocss就使用unocss，需要复用或者无法通过原子类实现才使用 CSS 文件
3. 不要自己写组件尽量使用 `shadcn` skill 创建组件

## 样式约束

1. 简约现代化，参考 Apple 官网的高级感


